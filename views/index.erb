<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" type="text/css" href="<%= url("/css/pure-min.css", false) %>">
  <link rel="stylesheet" type="text/css" href="<%= url("/css/index.css", false) %>">
  <title><%= page_title %></title>
</head>
<body>
  <div id="page-title"><h2><%= page_title %></h2></div>
  <div class="sortby-selection-menu pure-menu pure-menu-horizontal">
    <ul class="pure-menu-list">
      <li class="pure-menu-item"><a href="<%= url("/files/" + path, false) %>?sort_by=title"         class="pure-menu-link">Title</a></li>
      <li class="pure-menu-item"><a href="<%= url("/files/" + path, false) %>?sort_by=title_reverse" class="pure-menu-link">Title(reverse)</a></li>
      <li class="pure-menu-item"><a href="<%= url("/files/" + path, false) %>?sort_by=date"          class="pure-menu-link">Date</a></li>
      <li class="pure-menu-item"><a href="<%= url("/files/" + path, false) %>?sort_by=date_reverse"  class="pure-menu-link">Date(reverse)</a></li>
      <% if upperpath %>
        <li class="pure-menu-item">
        <a href="<%= url("/files/%s?sort_by=%s" % [upperpath, sort_type], false)%>" class="pure-menu-link">../</a>
        </li>
      <% end %>
    </ul>
  </div>
  <table id="file_list" class="pure-table">
    <thead>
      <tr>
        <td>File/Directory</td>
        <td>Button</td>
        <td>Last Modified</td>
      </tr>
    </thead>
    <tbody>
      <% files_withmtime.each_with_index do |(f, mtime),index| %>
        <tr>
          <% if File.file? f %>
            <td class="file">
              <a href="<%= url('/get_file/' + f, false)%>">
                <%= File.basename(f)%>
              </a>
            </td>
          <% else %>
            <td class="directory">
              <a href="<%= url("/files/%s?sort_by=%s" % [f, sort_type], false) %>"><%= f + "/" %></a>
            </td>
          <% end %>
          <td class='buttons'>
            <a class='file-button pure-button' href='<%= url("/generate_mobi/#{f}", false) %>'>Generate</a>
          </td>
          <td class="mtime"><%= mtime %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

</body>
</html>

