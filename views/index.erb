<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" type="text/css" href="/css/pure-min.css">
  <link rel="stylesheet" type="text/css" href="/css/index.css">
  <title><%= page_title %></title>
</head>
<body>
  <div id="page-title"><h2><%= page_title %></h2></div>
  <div class="sortby-selection-menu pure-menu pure-menu-horizontal">
    <ul class="pure-menu-list">
      <li class="pure-menu-item"><a href="/files/?sort_by=title"         class="pure-menu-link">Title</a></li>
      <li class="pure-menu-item"><a href="/files/?sort_by=title_reverse" class="pure-menu-link">Title(reverse)</a></li>
      <li class="pure-menu-item"><a href="/files/?sort_by=date"          class="pure-menu-link">Date</a></li>
      <li class="pure-menu-item"><a href="/files/?sort_by=date_reverse"  class="pure-menu-link">Date(reverse)</a></li>
    </ul>
  </div>
  <table id="file_list" class="pure-table">
    <thead>
      <tr>
        <td>File/Directory</td>
        <td>Button</td>
        <td>Last Modified</td>
      </tr>
    </thead>
    <tbody>
      <% files_withmtime.each_with_index do |(f, mtime),index| %>
        <tr>
          <% if File.file? f %>
            <td class="file"><%= "<a href='/get_file/#{f}'>#{File.basename(f)}</a>" %></td>
          <% else %>
            <td class="directory"><%= "<a href='/files/#{f}'>#{f}/</a>" %></td>
          <% end %>
          <td class='buttons'>
            <%= "<a class='file-button pure-button' href='/generate_mobi/#{f}'>Gen</a>" %>
            <%= "<a class='file-button pure-button' href='/get_file/mobi/#{f}'>Mobi</a>" %>
          </td>
          <td class="mtime"><%= mtime %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

</body>
</html>

